export type scrollYMoveProps = {
  scrollYMove: string;
  windowHeight?: number;
};

export type backgroundProps = {
  backgroundImage?: string;
};

export type displayControllerProps = {
  show: boolean;
};
